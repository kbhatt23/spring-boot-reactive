package com.learning.springreactive;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import reactor.core.publisher.Flux;
import reactor.test.StepVerifier;

//@RunWith(SpringRunner.class)
//@SpringBootTest
public class SpringReactiveApplicationTests {

	/*
	 * @Test public void contextLoads() { }
	 * 
	 * @Test public void assertBasicFluxSuccess() {
	 * 
	 * System.out.println("============starting test "
	 * +"assertBasicFluxSuccess=============="); Flux<String> fluxStr =
	 * Flux.just("ram","sita","hanuman").log();
	 * 
	 * StepVerifier.create(fluxStr) .verifyComplete() ; }
	 */
}
